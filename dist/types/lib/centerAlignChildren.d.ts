import React from "react";
interface Props {
    center?: boolean;
    children?: React.ReactNode;
    className?: string;
    verticalCenter?: boolean;
    style?: React.CSSProperties;
}
export default function CenterAlignChildren(props: Props): JSX.Element;
export {};
