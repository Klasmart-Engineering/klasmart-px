import React from "react";
interface Props {
    handleClose: () => void;
    subtitleID: string;
    toolbarBtn?: React.ReactNode;
}
export default function DialogAppBar(props: Props): JSX.Element;
export {};
