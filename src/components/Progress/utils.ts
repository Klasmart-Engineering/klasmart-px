import makeStyles from '@mui/styles/makeStyles';

export const useButtonLoadingStyles = makeStyles({
    buttonLoading: {
        pointerEvents: `none`,
    },
    buttonLoadingContent: {
        opacity: 0,
    },
});
